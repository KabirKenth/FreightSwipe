# Base URL for the API. "/api" on the single-domain Vercel deploy;
# http://localhost:3001 when running the docker-compose stack.
REACT_APP_BACKEND_URL=http://localhost:3001

# Browser-visible by design — restrict it by HTTP referrer in Google Cloud Console.
REACT_APP_GOOGLE_MAPS_API_KEY=
